{
  "name": "saybooks",
  "version": "0.2.0",
  "description": "Saybooks — a conversational ERP where the agent and the UI are the same system: one command registry, one rulebook, one audit trail.",
  "type": "commonjs",
  "private": true,
  "bin": {
    "saybooks-mcp": "./mcp-server.js"
  },
  "scripts": {
    "start": "node server.js",
    "mcp": "node mcp-server.js",
    "demo": "node demo.js",
    "test": "node test/contract.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "@napi-rs/canvas": "^0.1.65",
    "better-sqlite3": "^11.10.0",
    "pdfjs-dist": "^4.10.38",
    "pdfkit": "^0.17.1"
  },
  "license": "AGPL-3.0-only"
}
